.include "macro.inc"
/* ============================================================
 * W64-A18  BLOB-vs-TU OWNERSHIP SWEEP -- SPLAT-LANE FILLER ONLY
 *
 * This segment's data is OWNED by
 *     build/recon/game/psx/trackspec.cpp.o
 * (section .sdata, retail 0x8013db90..0x8013db9c), whose own bytes were
 * proved byte-identical to retail before the cut (ownmap2 E1-E5, with
 * R_MIPS_32 relocations RESOLVED against the retail VA map).
 *
 * The RECON-lane linker script must link that object's .sdata here and must
 * NOT link this file -- otherwise every label below is multiply defined.
 * It stays in the tree only so the legacy splat-lane script
 * (linkers/nfs4.ld, which links build/src/**, where no recon object exists)
 * keeps producing a byte-identical image.
 * ============================================================ */

.section .sdata, "wa"

/* Generated by spimdisasm 1.40.2 */

nonmatching TrackSpec_gPrevSpec

dlabel TrackSpec_gPrevSpec
    /* 12E390 8013DB90 00000000 */ .word 0x00000000
enddlabel TrackSpec_gPrevSpec

nonmatching TrackSpec_gCurrentSpec

dlabel TrackSpec_gCurrentSpec
    /* 12E394 8013DB94 00000000 */ .word 0x00000000
enddlabel TrackSpec_gCurrentSpec

nonmatching TrackSpec_gMaxSpec

dlabel TrackSpec_gMaxSpec
    /* 12E398 8013DB98 00000000 */ .word 0x00000000
enddlabel TrackSpec_gMaxSpec
