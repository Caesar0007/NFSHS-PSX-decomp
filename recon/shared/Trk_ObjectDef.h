/* Trk_ObjectDef -- the one definition of this type, shared by 11 headers (moved verbatim by
   tools/psyq_pipe/dedup_types.py; every former copy now includes this file at the same place). */
#ifndef NFS4_SHARED_TRK_OBJECTDEF_H
#define NFS4_SHARED_TRK_OBJECTDEF_H

struct Trk_ObjectDef { short id; u_char vertexCount, quadCount; };
#endif
