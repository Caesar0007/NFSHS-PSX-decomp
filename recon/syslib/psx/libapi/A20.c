/* syslib/psx/libapi/A20.c -- RECONSTRUCTED.  obj libapi.lib(A20.OBJ): StopPAD2 -- BIOS 0xB0:0x14 tail-call thunk.
 *   Retail LINKED this member (SYM FILE record, tail of the link order: libapi PAD.obj references it) and its final
 *   link then REMOVED it: PAD.obj's InitPAD / StartPAD / StopPAD / PAD_init were unreferenced, so nothing called this
 *   any more.  No B0:0x14 trampoline exists anywhere in the image.  Bytes: PsyQ 4.3 libapi A20.obj. */
#include "../../../lib/bios_thunk.h"

BIOS_THUNK_LINK_STRIPPED(StopPAD2, 0xB0, 0x14);
