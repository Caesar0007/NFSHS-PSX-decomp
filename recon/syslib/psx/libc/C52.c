/* syslib/psx/libc/C52.c -- RECONSTRUCTED.  obj libc.lib(C52.OBJ): free -- BIOS 0xA0:0x34 tail-call thunk.
 *   Retail LINKED this member (SYM FILE record, link index 433, between libsn WRITE.obj and libapi A12.obj) and its final
 *   link then REMOVED it: no `free` label and no A0:0x34 trampoline exist anywhere in the image.  Bytes: PsyQ 4.3 libc
 *   C52.obj.  (Until 2026-09-20 this file held an invented "Libc_c52" no-op at 0x80106E0C -- that word is the delay-slot
 *   nop of the function before EnableEvent; nothing referenced it and it had no retail address to be placed at.) */
#include "../../../lib/bios_thunk.h"

BIOS_THUNK_LINK_STRIPPED(free, 0xA0, 0x34);
