/* The earlier reconstruction split two command-property tables into this
 * synthetic TU.  SYM identifies a single BIOS.obj owner, and the retail image
 * places all four tables contiguously at 0x8013C00C..0x8013C20C.  Storage now
 * lives with the other two tables in drv.c; keep this empty compatibility TU
 * until the build manifest can retire it. */


