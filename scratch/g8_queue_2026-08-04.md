# 2026-08-04G — the w47-a7 §7 game -G8 probe queue, executed inline (20 objects)

Method: temporary `NFS4_PROBE_G` hook (a7's pattern) re-added to build.py for the probes and
REMOVED after wiring; whole-TU gate (verify_asm over every MATCH_PROGRESS fn of the unit) at
baseline vs `g_value=8`; per-fn verdicts compared. Positive control: force.cpp (wired 8, 9/9
PASS) probed back at G4 → 7 PASS / 36 diffs = w47-a9's census ladder exactly.
Wiring bar = w47-a8's: net diff improvement + ZERO PASS regressions + reproduced 2×.

## WIRED (7 TUs, PER_TU_FLAGS g_value "8") — all reproduced 2×, re-gated hookless after wiring

| TU | base | G8 | note |
|---|---|---|---|
| game/common/copspeak | 24 PASS / 28 diffs | **27 PASS / 0 — TU SEALED** | RadioStaticInit/RadioStaticActive/RadioStaticSquelch → PASS |
| game/common/audioeng | 5 / 62 | **7 / 50** | AudioEng_Pause + AudioEng_CleanUp → PASS |
| game/common/input | 4 / 69 | **6 / 39** | Input_StartUp + Input_WingCommandMode → PASS |
| game/common/hudpmx | 2 / 21 | 2 / **5** | −16 |
| game/common/nfs3 | 8 / 128 | 8 / **119** | −9 |
| game/common/r3dcar | 22 / 186 | 22 / **183** | −3 |
| game/psx/weather | 21 / 106 | 21 / **104** | −2 |

Project: 2744 → **2751 / 3496 = 78.7%** (MATCH_PROGRESS regenerated).

## NOT WIRED — gate evidence AGAINST -G8 (census placement signature FALSIFIED for these)

* **replay**: +158 diffs, FOUR PASS regressions (ResetReplay/GetInput/GetInterfaceKey/
  ReplayChooseCamera) — decisively -G≤4 despite its `8..16` signature.
* **audiocmn**: −61 diffs BUT LoadGameSamples PASS→FAIL. **simqueue**: 1 convert
  (SetCurrentInput) + 1 regression (GetCurrentInput). **draww**: 1 convert (NightCalc) +
  2 regressions (SetAnimationTime/DrawObjectTransform).
* **G5/G6/G7 ladder on all three mixed TUs = byte-identical to G4** ⇒ every sensitive symbol
  is EXACTLY 8 bytes, and their oracles MIX gp-rel and absolute 8-byte refs — no single -G
  value fits the object. NAMED ANGLE: per-symbol, not per-TU — likely the regressing fns
  reference 8-byte symbols OWNED ELSEWHERE (an extern's home TU had a different -G), so the
  fix would be declaration-shape/ownership (cf. the w47 unsized-array law), not a flag.
* **drawc**: +45 diffs, no conversions — worse, not -G8.

## INERT (no gate delta at all — left at default, no evidence either way)

aih_play · dashhud · mpause · render · hrzsku · overlays · psxcontroller · textureprocess
(their `≥8` placement signatures are consistent with -G8 but nothing measurable today; re-probe
when new fns in these TUs approach PASS).

## HAZARD PAID

**PER_TU_FLAGS is a dict literal — a DUPLICATE key silently discards the earlier entry.**
r3dcar's fresh `{"g_value": "8"}` was eaten by the pre-existing `{"jtbl_at_fusion": True}`
entry 100 lines below (wired re-gate read 186, probe said 183 — the 3-diff discrepancy was the
tell). Merged into one entry; added a Counter audit (27 keys, 0 duplicates). After ANY wiring,
re-gate WIRED (hookless), not just probed, and diff the numbers.
