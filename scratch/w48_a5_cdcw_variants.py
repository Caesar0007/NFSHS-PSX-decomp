BASE = '    int old = CD_cbsync;\r\n    int count = 4;\r\n\r\n    while (count--) {'
WATCH = ['CdControl']
VARIANTS = [('A base', '    int old = CD_cbsync;\r\n    int count = 4;\r\n\r\n    while (count--) {'), ('B up-front sentinel', '    int old = CD_cbsync;\r\n    int sentinel = -1;\r\n    int count;\r\n\r\n    for (count = 3; count != sentinel; count--) {'), ('C sentinel decl first', '    int sentinel = -1;\r\n    int old = CD_cbsync;\r\n    int count;\r\n\r\n    for (count = 3; count != sentinel; count--) {'), ('D literal -1 for-loop', '    int old = CD_cbsync;\r\n    int count;\r\n\r\n    for (count = 3; count != -1; count--) {'), ('F sentinel after count', '    int old = CD_cbsync;\r\n    int count;\r\n    int sentinel = -1;\r\n\r\n    for (count = 3; count != sentinel; count--) {')]
