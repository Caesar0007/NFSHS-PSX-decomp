BASE = '    intr = &Intr;\r\n    intr->c = 0;\r\n    c = *(volatile unsigned char *)&intr->c;   /* oracle genuinely RELOADS .c (no const-propagate) */\r\n    intr->ready = c;\r\n    intr->sync = 2;\r\n    CDREG0 = 0;\r\n    CDREG3 = 0;\r\n'
WATCH = ['CD_flush']
VARIANTS = [('A current', '    intr = &Intr;\r\n    intr->c = 0;\r\n    c = *(volatile unsigned char *)&intr->c;   /* oracle genuinely RELOADS .c (no const-propagate) */\r\n    intr->ready = c;\r\n    intr->sync = 2;\r\n    CDREG0 = 0;\r\n    CDREG3 = 0;\r\n'), ('F fence on intr', '    intr = &Intr;\r\n    __asm__("" : "=r"(intr) : "0"(intr));   /* w47 opacity/identity fence */\r\n    intr->c = 0;\r\n    c = *(volatile unsigned char *)&intr->c;   /* oracle genuinely RELOADS .c (no const-propagate) */\r\n    intr->ready = c;\r\n    intr->sync = 2;\r\n    CDREG0 = 0;\r\n    CDREG3 = 0;\r\n'), ('G fence before sync store', '    intr = &Intr;\r\n    intr->c = 0;\r\n    c = *(volatile unsigned char *)&intr->c;   /* oracle genuinely RELOADS .c (no const-propagate) */\r\n    intr->ready = c;\r\n    __asm__("" : "=r"(intr) : "0"(intr));   /* w47 opacity/identity fence */\r\n    intr->sync = 2;\r\n    CDREG0 = 0;\r\n    CDREG3 = 0;\r\n'), ('H fence after ready', '    intr = &Intr;\r\n    intr->c = 0;\r\n    c = *(volatile unsigned char *)&intr->c;   /* oracle genuinely RELOADS .c (no const-propagate) */\r\n    intr->ready = c;\r\n    __asm__("" : "=r"(intr) : "0"(intr));   /* w47 opacity/identity fence */\r\n    intr->sync = 2;\r\n    CDREG0 = 0;\r\n    CDREG3 = 0;\r\n')]
