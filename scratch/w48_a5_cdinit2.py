BASE = '    int retry = 4;\r\nloop:\r\n    if (_cd_event_init() == 1) {\r\n        CD_cbsync        = (int)_cd_event_sync;\r\n        CD_cbready       = (int)_cd_event_ready;\r\n        CD_cbread        = (int)_cd_event_read;\r\n        CD_read_dma_mode = 0;\r\n        return 1;\r\n    }\r\n    retry--;\r\n    if (retry != -1) goto loop;\r\n\r\n    printf("CdInit: Init failed\\n");\r\n    return 0;'
WATCH = ['CdInit']
VARIANTS = [('E current', '    int retry = 4;\r\nloop:\r\n    if (_cd_event_init() == 1) {\r\n        CD_cbsync        = (int)_cd_event_sync;\r\n        CD_cbready       = (int)_cd_event_ready;\r\n        CD_cbread        = (int)_cd_event_read;\r\n        CD_read_dma_mode = 0;\r\n        return 1;\r\n    }\r\n    retry--;\r\n    if (retry != -1) goto loop;\r\n\r\n    printf("CdInit: Init failed\\n");\r\n    return 0;'), ('G invert-goto-fail', '    int retry = 4;\r\nloop:\r\n    if (_cd_event_init() != 1) goto fail;\r\n    CD_cbsync        = (int)_cd_event_sync;\r\n    CD_cbready       = (int)_cd_event_ready;\r\n    CD_cbread        = (int)_cd_event_read;\r\n    CD_read_dma_mode = 0;\r\n    return 1;\r\nfail:\r\n    retry--;\r\n    if (retry != -1) goto loop;\r\n\r\n    printf("CdInit: Init failed\\n");\r\n    return 0;'), ('H invert + shared ret var', '    int retry = 4;\r\n    int r;\r\nloop:\r\n    if (_cd_event_init() != 1) goto fail;\r\n    CD_cbsync        = (int)_cd_event_sync;\r\n    CD_cbready       = (int)_cd_event_ready;\r\n    CD_cbread        = (int)_cd_event_read;\r\n    CD_read_dma_mode = 0;\r\n    r = 1;\r\n    goto out;\r\nfail:\r\n    retry--;\r\n    if (retry != -1) goto loop;\r\n\r\n    printf("CdInit: Init failed\\n");\r\n    r = 0;\r\nout:\r\n    return r;')]
