/* Diagnostic-only: original free prototypes after isolating pre-existing bridge collisions. */
#include "aitriger.free_before.h"
int (*p875_before_0)(void *op1, void *op2) = &AITrigger_Compare__FPCvT0;
