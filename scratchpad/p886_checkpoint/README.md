# P886 verified continuation (uncommitted)

## Source restoration

gHelpShapes now uses its native scalar PTR contract across nine declarations
and13 actual consumer TUs. Removed52 false outer indices and five unrecorded
slot carriers. The real noise local is retained because native52dbb6 names it.
All three base objects and ten regional fingerprints are unchanged; base46/46
PASS, branches0,16 native reference words exact, zero new native SLD merges.
The regional13 selectedPASSes and23 inherited misses are unchanged. Separate
regional fedialog gHelpShapesA and drawshp gHelpShapes_v aliases remain open.

FEInput's private ticks interface and base/JPN consumers use scalar ticks,
preserving the single load into native local tick. Both entire objects remain
identical; base4/4, all debug twins and changed-function branches pass. JPN's
existing four-diff/older branch problems remain explicitly recorded, unchanged.
Four raw reference words (two per build) agree. No polling qualification was
added or removed in this graph.

Fog_ReadFogKeys previously passed the normalized instruction check while
selecting the wrong filenames in three branches. Raw instruction stepping,
literal bytes and Redec prove S.fog/N.fog/W.fog/.fog in the actual branch order.
Correcting the ordinary strings changes only three compiled .sdata bytes.
All instructions,148 relocations, symbols and other sections remain identical.
The real i/INT/REG17 declaration now occupies native scope800E0FD8..800E1004;
readmem/strspc/numkeys declaration order also agrees with the native records.
That final declaration-order refinement was re-gated after the full build and
proves the identical complete object; it does not invalidate the linked output.
The extra empty native nested-block records remain a separate fidelity detail.

## Data/source ownership and actual reference repair

TextureProcess's G8 .sdata includes40 bytes of literals before28 bytes of
globals. The old fragment counted that prefix twice and started the source
section40 bytes late. The full source section now starts8013DB4C; the raw
strings move from r21 to existing oracle-only o34. The source order matches
all68 native bytes. Three strict lanes preserve the84-byte surrounding window
and every raw label, without allocating invented literal variables.

Removing that displacement restores following small-data addresses. The
existing160-byte vars.obj source now owns all40 native cells at8013DC64..DD04;
its raw copy is src-only. Its strict392-byte window preserves90 labels and
shapeext's internal pointer. All40 vars and six fog globals bind natively.
Across38 consumers, all438 checked named relocation words are now raw-exact
(437 were wrong before). The separate complete GP audit repairs29 more native
targets:1462->1433 wrong-but-in-range, no correct targets regressing. These
counts overlap and must not be summed. Previous50 table reference fixes remain.

## Full regression and remaining limits

All three full builds passed without skipped/failed TUs. All518 final source
objects reproduce byte-for-byte. The only intentional pre-round normal-object
change is TextureProcess's three filename bytes, proved independently. All446
functions across19 protected base TUs PASS with zero branch discrepancies.
The seventeen data-owner windows, raw instruction/data traces, both standing
relink lanes, vtable/source policy and CI-style objdiff report generation pass.

Known blob duplicates898->852; REAL duplicates, phantoms and referenced
unresolveds remainzero. The2381-site GP census has no overflow/undefined/
encoding errors;1433 wrong-but-in-range targets still need correction. The
broader diagnostic link still has124 allocated overlap pairs,28348 appended
main-data bytes and the sbss type warning. This is not a valid flattened final
image or proof of fully original source.

Fresh frontend/psx audit has85 declaration-clean functions but53 source-only
carriers (54 before this round) and18 blob-backed globals. Game/psx has392/395
declaration-clean functions, three extra names and394 source-only carriers.
These reports are review evidence, not waivers or original-source completion.

The read-only data investigation also found ISO9660's real9216-byte BSS at
8013F624 instead of native8014487C, affecting42 section-relative references.
Its initialized16-byte data is already placed correctly; merely retiring that
raw duplicate would not fix the BSS. Other reviewed new/simqueue/replay raw
copies are duplication debt with already-correct active bindings. Their
NOBITS/PROGBITS distinctions and alias-use census are preserved for the next
round in ../p886_owners/READ_ONLY_FINDINGS.md.

Evidence: final_receipts.json, full_validation_receipts.json, the two strict
audit reports, and ../p886_ghelp, ../p886_feinput, ../p886_vars, ../p886_owners.
Current source/header/tool/raw backups and historical results are retained.
No new asm, volatile, register pins, compiler flags, fabricated names or output
rewrites were introduced. No commit/push was performed; the full goal is active.
