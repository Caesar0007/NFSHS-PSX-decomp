# P911 conditional oracle fallbacks — generation finding

This remains a private proposal, not production. The11 exact assignments in
`linkers/undefined_syms_auto.txt` are generated by splat, not normal build.py:

- `Makefile:19`: `split` runs `tools/gen_symbols.py`, then splat and fix_gte.
- `configs/nfs4.yaml:27` names the auto-symbol file.
- `C:/Temp/splat-main/src/splat/scripts/split.py:474` writes each selected
  undefined symbol as an unconditional `name = VA;` assignment.
- Its `write_undefined_syms_auto` selects referenced, undefined non-functions.
- Normal build/gen_ld read the checked-in auto file; CI does not run splat.

Thus another explicit `make split` would overwrite the11 private PROVIDEs.
Do not call the proposed text edit generation-stable. No generator was run or
changed in this diagnostic. Root was asked to choose a narrow fail-closed
metadata contract or an idempotent post-split fix before any eventual landing.

`tools/relink.py:145` already accepts optional `PROVIDE(...)` in its
linker-resolved-name parser. Its `ld -r` gate intentionally omits final
placement scripts, so it classifies such unresolved names from metadata.
`tools/undefcall_audit.py:72` recognizes only hard assignments, but these11
symbols are storage-only and have no reconstructed callers/foreign references;
that call-oriented parser is not a reason to export false native globals.

The child receipt separately exercises the real oracle HUD object with these
existing fallbacks. Source/oracle references must remain resolved; no new name,
VA, external C declaration or raw storage is invented.

The candidate also fails the independent no-increased-overlap check. Resolve
that layout prerequisite before implementing any fallback-generation change.
