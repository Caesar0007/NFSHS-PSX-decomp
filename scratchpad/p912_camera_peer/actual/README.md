# P912 actual Camera —268/268 references verified

The landed normal build passes the complete bounded Camera check:

- 204 source and64 foreign references have exact native owner/addend and
 full encoded words (140 HI16,128 LO16).
- All268 detailed records equal the frozen private proof after mapping only
 the Camera object path from private candidate to normal production path.
- All ten native cells and1404 source/ROM/CPE/linked bytes are exact.
- All nine retired r06 aliases remain absent and unreferenced.

Actual noncomment Camera source tokens equal the accepted candidate.
The three reviewed headers are byte-identical to the source proof. Normal
object payloads, ordered symbols/relocations and section metadata equal the
private candidate except the precise STT_FILE path and its string-table
encoding. That exception is checked explicitly, not broadly normalized.

Normal Camera object:
`3ce010d08f2c756702920ed391d61697c2bc015e71b89554530d2a0eaf924641`.
Normal ELF:
`415cde9ff28286ae6d4bab1c0b05eb3f51aa3cc118d29158242e2fbf826f7f8c`.
Normal map:
`3b5b518f59b1273737539ee51f7a241155be6e43a0b2fba2be93613c7f1863a3`.

All695 current inputs are hash-pinned and rechecked unchanged; no historical
normal-object hashes are assumed after landing. Reads use precise actual
input-map/output sections. External/BSS placement, Collide/GenericPMX,
full318 transfer and complete-image geometry remain parent scope.

No builds, source edits, normal-output writes or object rewrites occurred.
Only this new namespace was written; previous private/source/census receipts
remain intact. `results.json` contains all268 records and full input hashes.
