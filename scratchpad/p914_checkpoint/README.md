# P914: Horizon recovered and matching-only identity exemptions exposed

Verified local source/auditor checkpoint. DrawW remains a separate private
proposal and is not included in the landed results below.

## Source restoration

Horizon_InterpolateLineSCoords now uses the original root `i` and else-local
`p = *percentage`, confirmed by native SYM/raw and the matched NFS2 PC twin.
Direct signed product /65536 expressions replace the false named product
temporary. The unproved `pv` and artificial index-absorption expression are gone.

**80/80 raw words; all three native scopes and local/parameter homes;
SLD 0/232 -> 0/0.** All 22 HrzSku functions and 170 branch checks remain PASS,
and all 21 neighbor contracts are preserved. The normal object is literally
unchanged. Only the target body/comment changes; exact outside-target bytes,
including original mixed line endings, are restored and verified.

## Honest identity reports

The tracked read-only auditor adds `--strict-source-identities`, retaining
matching-only local/global/split-array carriers in the review queue. Default
behavior is unchanged. This does not replace raw value-role, SLD, foreign-header,
initializer or runtime-layout checks; retained primary categories still need
their own evidence.

Tests: **33 policy assertions, four real-source cases, four complete-directory
legacy/default report-and-stdout comparisons**, plus default/strict/default
state reuse. Strict mode exposes 1,406 extra-local review items across the four
frontend/game directories, compared with 59 under old exclusions. These are
review findings, not new code regressions or an automatic proof of bad names.

See [reporting guide](../root_sym_audit/STRICT_SOURCE_IDENTITY_REPORTING.md),
`audit_reports.json` and `../p914_identity_review/EXECUTED_RESULTS.md`.

## Regression boundary

All 695 normal link inputs, complete ELF/map and the freshly generated matching
report are literally unchanged. Both scoped relink lanes, all existing native
owner contracts, vtable and source-policy audits pass. No new invented source
identifier, asm, volatile or postcompile instruction/object rewrite was added.

Backups, the initial context-EOL failure, guarded formatting repair and actual
repeat checks are preserved. Tool backups/comments accompany the opt-in mode.
Current core receipts are `horizon_actual.json`, `source_checkpoint_closure.json`,
`audit_reports.json` and `final_receipts.json`. `publication_manifest.json`
excludes unrelated edits, generated outputs and all private DrawW experiments.

Changes are not committed or pushed. The pending publication question covers
the earlier P904-P913 scope, not this newer checkpoint. The full original-source
and SYM/SLD goal remains active and incomplete.
