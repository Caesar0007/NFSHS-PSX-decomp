# P915 — DrawW native declaration and storage restoration

DrawW remains **35/35 PASS, 361 branch checks** while restoring three incorrect
foreign array bounds and the two missing identity-matrix diagonal values.
Its 14 native storage objects now occupy the exact **1,776-byte .data and
36-byte .sdata** runs. Both real eight-byte static arrays retain their original
names and scope. No ordinary base-function body, invented name, asm, volatile,
padding object or postcompile rewrite was added.

The complete source/interface/data changes use the installed compiler's G8
default. Native threshold evidence is8<=G<24, not unique historical flag tokens.
Night's compatible incomplete caller bound is instruction-backed, not uniquely
recoverable source spelling; the native owner stays[2]. These qualifications
remain open instead of being called original-token proof.

## Actual verification

- All35 base instruction streams and361 internal branch checks pass.
- All35 prior local/declaration/lexical/SLD contracts are preserved.
- USA's six stale compatibility-view families are repaired using existing
  native declarations. The actual regional pipeline reproduces the independently
  checked35-function/359-branch USA object, apart from its exact FILE path.
- All14 cells and1,812 initialized bytes match SYM/MAP/CPE/ROM.
- All265 own/foreign selected linked references match their raw native words.
- All2,091 strict owner controls pass:30 valid and2,061 malformed rejections.
- Both standing relink lanes, vtable and source-policy checks pass.
- The fresh full matching report is byte-identical to P914: no PASS regression.

The final link fixes34 wrong GP-relative targets (**1006->972**), preserving all
1,372 previously correct sites. It removes1,812 duplicate allocated bytes and
reduces raw duplicate symbols698->686. No code moves or new overlap is created;
all1,280 executable-word changes are exact existing relocation encodings.
All3,896 previously native public records,109 FILE-qualified LOCAL records and
3,238 native function records remain intact. Remaining layout debt is not hidden.

## Evidence and boundaries

`baseline.json`, `actual_gate.json`, `actual_debug.json`, `actual_usa.json`,
`actual_controls.json`, `integration.json`, `relink.json`, `final_receipts.json`
and `publication_manifest.json` cover actual paths. Independent private and
actual reference/image checks are under `../p914_draww_storage` and
`../p914_draww_peer`. Backups precede all edits, including protected tools.
`source_formatting.json` records formatting-only recovery of unchanged EOLs.

USA raw checks are not a fully linked USA byte-match claim. Inherited regional
source VA comments remain base-image breadcrumbs. DrawW's earlier source-local
and SLD debt is preserved, not certified as fully recovered; the strict game/psx
review queue remains258 clean functions and395 extra-local findings.

**Local only: P904-P915 are not committed or pushed.** The P898-P903 approval
was already fulfilled by7aef1e7c. Publication scope excludes unrelated user files,
generated outputs and failed experiments. The original-source/SYM/SLD goal
remains active and incomplete.

Publication preflight found that remote main has advanced by two commits:
40a6597a (nullfunc fixes) and82005b8a (obsolete eaclib.h removal). The fetched
remote tip is82005b8ada6564053adc26dfbb8529fbf2f3f325. Neither commit overlaps
the60 scoped checkpoint paths, but their dependency changes still require
integration and regression checks before publication. Only fetch/read-only
inspection was performed: local HEAD remains7aef1e7c, the index is empty, and
no merge, reset, commit or push occurred. Earlier P898-P903 is an ancestor of
the current remote main. A new publication question explicitly covers P904-P915.
