# P915 actual normal-path DrawW peer proof

GREEN for actual normal ELF `a1a26c7c...`, map `9be26f7a...`, and DrawW object
`fe3e6a5b...`. The entire object equals the accepted private object except the
explicitly checked scratch-to-production FILE pathname.

All **265 actual linked reference words** (70 own +195 foreign) match native
raw instructions, actual selected targets, original interior addends and caller
PCs. All14 native data cells and1,812 initialized bytes match source, ROM and
complete CPE coverage. The two LOCAL tables retain production FILE identity,
including offsets.48. Both retired raw aliases are absent and unreferenced.

Every selected reference/cell row also equals the frozen private proof after
only the exact input-object/FILE path conversion. Complete actual response-file
reference census agrees; no per-symbol forced-native address substitution.

Original P914 receipts are preserved. This is the bounded selected-owner proof;
parent handles full-image transfer. No compilation or writes to production or
normal artifacts. Full hashes and rows: `review.json`; checker: `review.py`.
