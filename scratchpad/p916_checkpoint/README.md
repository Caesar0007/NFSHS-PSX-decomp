# P904-P916 verified restoration checkpoint

This checkpoint publishes the accumulated native source, SYM/SLD and data-owner
restoration after incorporating remote main through82005b8a. It excludes
unrelated edits, generated outputs, backups and unfinished experiments.

## Latest source recovery

`DrawW_GetAnimationTime` no longer declares the unproved `result` and `tick`
carriers. Its original `track` and else-local `maxTick`, all three native scope
spans, and complete SLD statement partition are restored. **33/33 instructions,
four branch targets, SLD0/73->0/0; all35 DrawW functions/361 branches remain
PASS.** The whole actual normal object is literally unchanged.

The minimum expression matches a reconstructed NFS2 macro's operand order;
the original NFS4 macro invocation is not uniquely recoverable from this proof.
All34 neighbors retain full declaration/scope/SLD and label-PC contracts.
Fresh strict game/psx coverage becomes259 clean functions and393 extra-local
review items, an improvement of one function and two removed findings.

See `../p916_animation_time/README.md`, `actual_verification.json` there,
and `../p916_animation_time_peer/review.json` for independent raw/SYM evidence.

## Accumulated verified changes

P904-P915 restored native source-owned data in Primate, FEI, Replay, AudioCmn,
GameSetup, HUD, Camera, GenericPMX, Collide, HrzSku and DrawW, with explicit
scope/SLD recovery and strict evidence-based owner validation. These are
bounded ownership/value/link repairs, not a claim that all source is original.
The opt-in strict identity auditor exposes matching-only exemptions honestly.
Historical checkpoint manifests and their receipts preserve each verified step.

The latest DrawW data round restored14 original storage objects /1,812 bytes,
fixed34 wrong GP targets (1006->972), and preserved every already-correct GP
site. Wider source, macro, declaration and layout debt remains open.

## Publication regressions

Remote commits40a6597a and82005b8a were integrated by fast-forward, without
overlapping the60 restoration paths. All seven remote-changed C TUs were
recompiled: their whole objects are literally unchanged. All694 normal inputs,
complete linked ELF/map and the fresh full matching report are unchanged from
P915. Both scoped relink lanes, all41 initialized and seven zero-owner contracts,
vtable and source-policy gates pass. No compiler output or debug label was
rewritten. User-owned scratch results and unrelated EOL edits remain untouched.

`closure.json`, `remote_recompile.json`, `relink.json`, the current declaration
reports, `final_receipts.json` and `publication_manifest.json` record the final
checks and exact selected scope. The existing locaterequest static-name/VA
disambiguation is explicit in the regression receipt, not silently skipped.

Private DrawC and centerline candidates remain unfinished and uncommitted.
Their agents are stopped. **The main goal is incomplete; pause after commit
and push was explicitly requested.**
