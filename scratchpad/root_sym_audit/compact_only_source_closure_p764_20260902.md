# Compact-only SYM source closure (P764, 2026-09-02)

## Scope

This receipt covers the 34 opcode-2/opcode-6 compact symbols that are absent
by VA from `configs/symbol_addrs.txt` and have no exact `Def`/`Def2` storage
record or debug-function span.  Earlier declaration-only audits did not see
this class.

The reviewed mapping is stored in `compact_source_homes.json`; the exhaustive
ledger consumes that manifest rather than treating incidental identifier hits
as declarations.  `raw_sym_ledger_p764_20260902.md/.json` reports:

- typed exact: 153;
- evidence-reviewed compact-only source homes: 34;
- unresolved compact-only records: 0.

## Newly restored storage

- `bigfilename @ 0x8013E940` is `locatbig.obj` file-static `char[64]`.
  Its unsuffixed compact name proves file scope, its 64-byte extent reaches
  `systemtasksubs`, and the archive/BSS order plus the adjacent locatbig-owned
  `bighandle`/`biglen` state identify the owner.  `locatbig.c` remains 7/7
  PASS.
- `Copspeak_gTimeString.308 @ 0x8013E0B0` is source identifier
  `Copspeak_gTimeString`, a `CopSpeak_Debug` function-local static `char[16]`.
  The numeric suffix is generated by GCC and is not part of the C++ name.  Its
  extent reaches `gBackList`; the Copspeak prefix and terminal COPSPEAK SLD
  region fix the owner.  `copspeak.cpp` remains 27/27 PASS.
- `systemtasksubs @ 0x8013E980` is now ordinary C storage:
  `static SYSTEM_TASK_SUB systemtasksubs[16]`.  The exact 0x100-byte extent and
  compact STATIC binding agree with NFS4, while the independently matched NFS2
  EAC source proves the four-word record shape.  The former file-scope asm BSS
  device is removed; `systask.c` remains 3/3 PASS and emits one local 0x100-byte
  BSS symbol.

## Shared SQV decompressor state

`SQVclue`, `SQVleft`, `SQVright`, `SQVs`, and `SQVd` are one coalesced common
block shared by `unbtree.obj` and `unref.obj`, not five unexplained globals.
The declarations now use the NFS2-confirmed pointer types and the exact compact
BSS order:

| Offset | Source identifier | Type |
|---:|---|---|
| 0x00 | `SQVclue` | `signed char *` |
| 0x04 | `SQVleft` | `unsigned char *` |
| 0x08 | `SQVright` | `unsigned char *` |
| 0x0C | `SQVs` | `unsigned char *` |
| 0x10 | `SQVd` | `unsigned char *` |

Both rebuilt objects emit those exact offsets.  The duplicate tentative
declarations are retained as an evidenced assembler-compatibility carrier:
plain `extern` declarations make GNU as expand `chase` accesses to HI16/LO16,
whereas the common declaration emits the retail one-instruction GP-relative
access.  `unref.c` remains 2/2 PASS and `unbtree.c` remains 1/1 PASS after the
pointer cleanup.

## Compiler-generated local suffixes

The two Newton records `dummy.124` and `dummy.133` now use ordinary source
identifier `dummy`; the forced `asm("dummy.N")` source labels are removed.
Their existing section attributes are link-placement anchors only, preserving
the independently proven `0x8013E0D8` and `0x8013E0E8` BSS seams.  The affected
functions remain PASS at 200 and 275 instructions.  Other numeric-suffix rows
(`lastX`, `lastY`, `resethud`, `tick32`, `failtime`, `countdownTick`, `corrPt`,
`lastOppVector`, both `strspc` objects, `fogstrspc`, and `flareextra`) already
have ordinary source identifiers/scopes and are now explicitly recorded in the
manifest.

## Verification

- `locatbig.c`: 7/7 PASS.
- `copspeak.cpp`: 27/27 PASS.
- `nasync.c`: 23/23 PASS.
- `stream.c`: 32/32 PASS (P759).
- `systask.c`: 3/3 PASS.
- `unref.c`: 2/2 PASS.
- `unbtree.c`: 1/1 PASS.
- Newton affected functions: 200/200 and 275/275 instructions.
- Both relink lanes: GREEN, zero real duplicates, zero hidden phantoms, zero
  relocation-referenced unresolved symbols.  Receipt:
  `compact_source_closure_relink_p764_20260902.json`.
- Full build completes.  Its existing project-wide mismatch remains
  1,239,008 generated bytes versus 1,239,040 retail bytes (size delta -32);
  this receipt does not claim whole-image equality.
