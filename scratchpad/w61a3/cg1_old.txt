        if (rslt != 2)
            break;
        retry = retry + 1;
        if (retry >= 4)
            break;
    }
    if (rslt == 0)
        rslt = 5;
    return rslt;
}

/* @0x800FBE20 : MemCardDeleteFile -- synchronously erase a named file. */
