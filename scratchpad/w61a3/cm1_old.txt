    fd = open(devname, 1);                       /* probe: does it already exist? */
    if (fd >= 0) {
        close(fd);
        return 6;                                /* already present */
    }
