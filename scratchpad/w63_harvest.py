#!/usr/bin/env python3
"""Append the W63 15-series harvest to the catalog."""
P = (r'C:\Users\Vyacheslav\.claude\projects\C--Temp-claud\memory'
     r'\reference_asm_pattern_catalog.md')

H = """

### -> W63 HARVEST (2026-08-15, THE CORPUS+DEVICE WAVE: 20 Opus [17 belts + idiom-miner + link-campaign + production-engineer]; board 3325->3340 = 95.7%; +22 gate seals (8 DUAL-LANE incl 2 production-bug fixes in gate-PASSing fns), 4 TUs completed (cdcont 19/19, aispeeds 29/29, audioclc 18/18, aistate 52/52), ~28 stale rows cleared, 6 tools promoted)
**15A -- THE FOREIGN-OPERAND FENCE (the multi-wave wanted device, FOUND then CORRECTED):** `__asm__("" : : "r"(OTHER_live_value))` = +1 REG_LIVE_LENGTH for EVERY pseudo live across it, +1 REG_N_REFS only on ITS operand, 0 bytes (live/ref axes separated; operand choice is a dial). BOUNDING LAW: flow.c:1594's live-len bump sits in the class-'i' arm @1399 => live length counts REAL RTL INSNS ONLY -- block scopes / do-while are ref dials NEVER live dials. 🔴A16's CORRECTION (quantified): REG_LIVE_LENGTH IS NOT THE ALLOCATION WINDOW -- find_free_reg scans [birth,death); live-len only feeds QTY_CMP_PRI = SERVING ORDER. The fence is a PRIORITY-ONLY dial: inert when the target is already first-served (check -dl serving order first) or when the residual is an AVAILABILITY loss (A10 Report, 9 placements inert as predicted). CHEAPEST PRE-GATE (A11): COUNT THE TARGET BLOCK'S QTYS -- blocks of 1-3 qtys skip the qsort entirely (the 14C ladder bug) so NO priority dial can fire there by construction. 2nd device (unwitnessed): a REG_EQUIV note DOUBLES live length at zero refs (global allocnos only). First customers: BuildChunkObjectFacets 19->8 count-exact (SEALED with it).
**15B -- ALLOCATOR/CSE LAWS:** 🔴🔴THE W45-A FORMULA WAS WRONG: gcc-2.8.1 global.c:600 + local-alloc.c:1727 = floor_log2(refs)*refs*SIZE/live -- SIZE MULTIPLIES, no minus term, no negative priorities, the 2-ref live-length inversion does not exist (HrzSetPsxMatrix w50 verdict retired). THE FENCED NAMED CONSTANT IS A DIFFERENT LEVER than a plain named temp (const-propagated back = provably inert) -- a falsified plain-temp receipt does NOT close the axis (3 independent landings). INVERTED 13B: when the residual is an EXTRA copy delete_noop_moves won't remove, identity-launder the CARRIER (cse can't prove the source pseudo) -- sealed _dirFailAuto after 5 waves of value-spellings; pointer-launder variant = alias barrier. IDENTITY LAUNDER IS DIRECTIONAL: buys a register but BLOCKS loop.c's invariant hoist (gate both directions). cse CONSTANT-SHARING substitutes at the DEF: the laundered local must be BORN BEFORE the competing materialization; if the shared copy filled a load-delay slot the relocation costs a nop => reject (FntPrint sealed / _dws rejected by the same law). CdControl triage: a 10th call-crossing global allocno hits CALLER_SAVE_PROFITABLE and gets $t0 -- name the constant in the PREAMBLE, position is the dial (first statement PASS, one later 9, in-loop inert). 3-way rotations: price as ONE floor_log2 promotion FIRST (A8 refuted a 2-wave 3-qty-rotation verdict: they were GLOBAL allocnos). W46 STORAGE-SCOPE LAW: one fn-scope scratch = one barred global allocno; block-scope ONLY the post-loop use (DIRECTION is the whole dial -- screenmain 110->12). Two pseudos dying on the same insn are provably un-reorderable by any fence (needs live > 5*gap).
**15C -- SCHEDULER/LOOP/SHAPE LAWS:** 🏆11B ARG-EMISSION CLASS CLOSED: calls.c:1900 final hard-reg loop is ascending-i + SCHED_GROUP_P-welded; precompute (:1674) can't discriminate same-shaped MEMs at -O2 => EMISSION ORDER IS ARGUMENT-LIST ORDER, retail arg4-first source-UNREACHABLE (one row x9, libmath -30). 🏆loop.c GIV-BENEFIT RAZOR: benefit==0 zeroes the whole product -- the ONLY input is the giv expression's RTL OP COUNT (any 3-op spelling of 9*i clears it; sealed Draw321Num, closed the w62 outer-additive-0 question). MOVABLE-EXISTENCE KILL: loop.c builds a movable only for n_times_set==1 -- assign in BOTH arms and the unaffordable hoist never exists (A12 76->64; A16's arm-duplicated variant is barrier-free). ALIAS-DEPENDENCE DIAL ON PRIM STORES: COMPONENT_REF spelling sets MEM_IN_STRUCT_P, un-chains casted prim stores from frame slots (Tach -10; SUPERSEDED the w50-a1 statement-order table -- the order sensitivity WAS the aliasing chain). ALARM-AS-ONE-STRUCT (A5, sealed CD_sync/CD_ready/CD_datasync): N consecutive same-typed globals always written together = a candidate struct VIEW; the alias class alone is a codegen dial (sotn+rage-racer both model it). 🏆cross_jump LABEL-BONUS UN-MERGER (A10, production bug in a gate-PASSing fn): find_cross_jump jump.c:2568-73 spends a free --minimum on any CODE_LABEL => a 1-insn return tail cross-jumps and redirects the guard INVISIBLY to verify_asm; cure = void fence between label and tail (jump.c:2632-35 loses on volatile ASM_OPERANDS), zero insns. FUNNEL NEEDS if/else ARMS: a COND_EXPR targeting a MEM stores in both arms, never reaches a shared store (W62's 'non-additive' was the ternary's fault; TireForces 49->21). FIRST-EXPANDED-COMPONENT-OWNS-THE-lo_sum: multi-field read of one global struct -- only the first component folds; base-reuse appears iff .x is last (the whole extra-lui class). combine_regs ELIGIBILITY != PRIORITY (local-alloc.c:470-77 + :1866): retail's fresh dest = the REG_N_DEATHS==1 bit; fences/live/ref dials predicted AND measured inert. Fence-after-the-consumer for a delay-slot constant (06B boundary). Anti-LICM selective devices (A16 Redraw, dump-verified): use-adjacency / identity-fence-as-2nd-SET / arm-duplicated assignment; T in [22.6,33.2).
**15D -- TEXT_MOVES/GATE MACHINERY:** 🏆A LABEL IS A MOVABLE LINE (pure line-regex engine) + multi-line drop_after UN-WRAPS .set noreorder blocks so maspsx supplies the empty slot => the reorg TARGET-STEAL class is TEXT_MOVES-reachable (HeliCam 12->PASS 443/443 via 4 rows incl 2 label moves). ANCHOR LAWS: regex anchors use NUMERIC registers ($4,$2) never ABI names -- the W62 Exist_cb row NEVER FIRED for this and read as INERT; 'after' binds the FIRST match fn-wide -- lookahead-pin AFTER anchors too, not just takes; build.py is CRLF -- multi-line literals need \\r\\n; assert count==1 (sibling fns share byte-identical runs). 🔴SEMANTICS-VERIFY EVERY SPLICE VIA OBJDUMP: a count-exact gate-PASS splice can be a DEAD-CODE BUG (the _padInitDirSeq trap: both macro halves after the branch, store never executes) -- the _dirCheck row was objdump-verified = retail's exact 11 words before wiring. _apply_text_moves searches take/after INDEPENDENTLY over the fn region: diff the generated .s, not just the score.
**15E -- PRODUCTION LANE + LINK CAMPAIGN:** PRODUCTION 98.12% (2916/2972 REAL=0; harness gaps 0, lane-divergent 0, ALL 46 residuals named shim-masked). GNU->ASPSX DIALECT SHIM default-ON in psyqproof (5 rules from a 42-construct probe); 🔴ASPSX expands div/divu $0 WITH the 4-word /0 guard (GNU-as: $0 = raw) => the reg-name rewrite must stay #APP-scoped or every compiler divide loses its guard; ASPSX has NO symbol-assignment form -- use a second label. maspsx .LCOMM GUARD LANDED both copies (file-statics stay local; gSwapFileName class dead; gate-neutral over 554 fns x2). brdist had THREE vacuity defects (unpadded targets {4,8}; .word IS an instruction -- spimdisasm renders every GTE op as .word; case-insensitive oracle lookup hit the wrong file): census now honest, 0 divergent on 100%-rows tree-wide => NO PulloverCam-class bug remains behind a green gate; a brdist row whose target is a nop = maspsx-vs-ASPSX label artifact, psyqproof arbitrates; REAL=1 on a gate-PASSing EA fn can be REAL jump-threading (SubmitRequest: genuine, fixed) -- classify case-by-case. LINK T1 (A19): ownmap.py PROMOTED = the E1-E5 decidable ownership gate (E5 window-bytes==ours = the anti-vacuity leg), data twin of tu_order_audit; probe_link CLIMBED 80.76->81.38 close-lane; 2 blob regions cut into 97 .ld pieces (byte-proven, reloc-identical); 2 RUNTIME BUGS from decoded retail bytes (fastrand fastRandom=0xF874AF01/randSeed=1; mpause menu id arrays); -G8 IDENTITY TELL class (8-byte objects in retail .sdata => g_value 8; wiring it TU-COMPLETED aispeeds free); region-2 (data_8010CCD4, E4 tails) is the next-best target; nfs4.ld is DEAD at HEAD (2077 .L jump-table labels); stale objdata.json costs ~1% -- scan.py immediately before every probe.
**15F -- CORPUS + PROCESS:** sotn-decomp src/main/psxsdk/ = a NEAR-COMPLETE MATCHED PsyQ SDK (zero INCLUDE_ASM: libcd bios/cdread/iso9660, libgpu sys+font, libetc intr+intr_dma, libc sprintf, libcard) -- 27/56 open syslib fns had corpus twins; psyz libmcrd/libmath = INCLUDE_ASM stubs (misses recorded: libmcrd, libmath, libpad -- 4th confirmation of 04X). 🔴CHECK THE $Id: REVISION BEFORE PORTING (sotn INTR = v1.73 vs our v1.76: all 6 ported angles falsified vs the only fully-matched body); matched bodies from another SDK generation are shape oracles ONLY where the CFG matches. A18's 4 catalog contradictions RECORDED FOR ADJUDICATION (13A allocator scope [decl order = spill slots only? conflicts with our W61-A20 lab measurement -- OPEN, control-measure pseudo numbers per case]; W46 param-copy park refuted by tenchu's fence-free local-copies cure [A2's BuildCustomObjectFacets = the named customer]; net-zero-pair row vs AGENT_GUIDE no-scaffolding [tenchu: diagnostic, not fix]; LoM's refs/spill_cost formula wrong -- do not adopt). New doc mines: lom gcc-272-matching-techniques.md (%lo(SYM±N) fold cures, shared-lui alias elimination), tenchu matching-cookbook/compiler-facts, PE2 +525 lines (T*-volatile vs volatile-T*, p=&b birth-order lever), PE1 maspsx purity gate = independent 2nd witness for 04C (keep maspsx-patch route closed). PROCESS: cross-TU twin porting is a FIRST-CLASS oracle (InGame_GetPSXPadValue 257->168 after 6 waves of dials never questioned the OR-tree nesting); predict-before-probe from .lreg/.greg alone sealed CdControl; heredoc collapse fired ~20x more (Edit tool or Write-script + ast.parse ONLY); Edit-tool CRLF normalization fired 3x (derive endings per anchor); apply.py's single .applybak goes stale across landings (fresh backup per landing); stage EXPLICIT paths only (a peer git add -A swept another agent's work); session-limit kills mid-belt are resumable via SendMessage with a re-baseline warning. TOOLS PROMOTED: fencesweep (mechanical void-barrier sweep = the W45 fence-fixpoint mechanisation), opcen (per-opcode census), ownmap, multidial, brdist fixed x3; alpha.py UNRUNNABLE as committed (imports a scratch-only module; posdiff.py is the working substitute) -- repair queued."""

data = open(P, encoding='utf-8', newline='').read()
assert 'W63 HARVEST' not in data
open(P, 'a', encoding='utf-8', newline='').write(H)
print('15-series appended')
