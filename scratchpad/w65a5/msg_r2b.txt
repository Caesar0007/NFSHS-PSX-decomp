w65a5: THE .L LOCALIZE CUT closed -- region 2 wired into BOTH linker scripts; the recon lane's undefined `.L` class is now ZERO (350 -> 0)

Region 2 (`rdata_80054548`, 36 jump-table records -> 61 pieces) was cut and
byte-proven in this lane, but its files were swept into a peer's `git add -A`
(commit 0ba59955, w65a8 -- the W64-A18 section-8 hazard firing one wave
later; content verified intact afterwards, only the message is not mine).
This commit lands what the sweep left behind: both linker scripts.

  linkers/nfs4.ld       -- the 61 region-2 pieces in blob order, plus BOTH
                           stems' `(.data)` and `(.bss)` entries restored;
  linkers/nfs4_recon.ld -- regenerated by scratchpad/w65a5/gen_ld.py with the
                           54 `_jNN` objects dropped from the object list AND
                           the script, each `_rNN` residual placed at its own
                           retail VA.

THE ZERO-SIZE SECTION DEFECT (region 1 shipped it, P6's image leg caught it):
the blob object's `.data`/`.bss` are EMPTY, so region 1 replaced those two
entries with a comment.  A zero-size input section STILL takes part in
SUBALIGN, so dropping the entries removed a 4-byte fill and shifted the whole
image.  Every piece is listed for all three sections now; re-aligning to 4 is
idempotent, so N zero-size sections cost exactly what 1 did.

P6 (control derived from the LIVE `linkers/nfs4.ld`, not a snapshot -- a peer
re-ordered the sdata_8013C54C pieces in that same file mid-run and a stale
control attributed a 96-byte phantom diff to this cut):
    .nfs4 image BYTE-IDENTICAL, 1236928 B both sides
    diagnostics 14 == 14, 0 normalised diff lines.

RECON-LANE LADDER (each rung measured on the tree as it stood at that moment;
multiple-definition 796 and reloc-truncated 10 UNCHANGED at every rung, so
every name that left is a `.L` and nothing else moved):
    no-cut control, pre-region-1 : 398 undefined (350 `.L`)
    after region 1               : 277 undefined (236 `.L`)
    after region 2               :  39 undefined (  0 `.L`)
    final, this tree             :  15 undefined (  0 `.L`)
      (its own same-instant no-cut control: 365 undefined, 350 `.L`)
The non-`.L` residual fell 48 -> 15 under peer belts (w65a6's blob-alias
sweep, w65a8's COMMONs), not under this lane.  W64-A19 priced the endpoint at
52; the `.L` axis is now closed and the remainder is A6's true-undefined
class -- untouched here.

GATES on the final tree: build.py --no-link RC=0 (508 recon objects, 94 rodata
piece objects) · tu_order_audit 508 objects / 0 inversions · probe_link 83.08%
(764382 B) / 202 of 442 byte-exact, CLIMBED from my own re-measured pre-cut
baseline 83.07% (764313 B) / 202 of 442 · ownmap over all three data regions
runs clean (this lane touches no `.data`/`.sdata` blob) · tugate spot-checks
on 8 owning TUs all equal to their pre-existing baselines.

Zero PASS->FAIL is STRUCTURAL: no recon/** source, no shared header, no
tools/*.py, and no recon TU includes asm/data/**.
