OLD = '  if (this->fArrowFadeDir < 0) {\n    this->mult = (short)(0x80 - (uint)(ushort)this->fArrowFade);\n    clampVal = (((((int)this->mult > 0) ? (int)this->mult : 0) < 0x40)\n                    ? (((int)this->mult > 0) ? (int)this->mult : 0) : 0x40);\n  }\n  else {\n    clampVal = 0x40;\n  }\n  this->mult = clampVal;\n  /* MATCH: zero-insn scheduling barrier keeps the SLD-1596 store before the\n     HorzVertLine argument setup instead of sinking it into the call slot. */\n  __asm__("" : : "i"(0));\n  this->HorzVertLine(ArrowLoc,false);'
VARIANTS = [
  ('Y1 no fence', OLD, '  if (this->fArrowFadeDir < 0) {\n    this->mult = (short)(0x80 - (uint)(ushort)this->fArrowFade);\n    clampVal = (((((int)this->mult > 0) ? (int)this->mult : 0) < 0x40)\n                    ? (((int)this->mult > 0) ? (int)this->mult : 0) : 0x40);\n  }\n  else {\n    clampVal = 0x40;\n  }\n  this->mult = clampVal;\n  this->HorzVertLine(ArrowLoc,false);'),
  ('Y2 store in both arms', OLD, '  if (this->fArrowFadeDir < 0) {\n    this->mult = (short)(0x80 - (uint)(ushort)this->fArrowFade);\n    clampVal = (((((int)this->mult > 0) ? (int)this->mult : 0) < 0x40)\n                    ? (((int)this->mult > 0) ? (int)this->mult : 0) : 0x40);\n    this->mult = clampVal;\n  }\n  else {\n    clampVal = 0x40;\n    this->mult = clampVal;\n  }\n  this->HorzVertLine(ArrowLoc,false);'),
  ('Y3 store in both arms, no clampVal', OLD, '  if (this->fArrowFadeDir < 0) {\n    this->mult = (short)(0x80 - (uint)(ushort)this->fArrowFade);\n    this->mult = (short)(((((int)this->mult > 0) ? (int)this->mult : 0) < 0x40)\n                    ? (((int)this->mult > 0) ? (int)this->mult : 0) : 0x40);\n  }\n  else {\n    this->mult = 0x40;\n  }\n  this->HorzVertLine(ArrowLoc,false);'),
  ('Y4 explicit false local', OLD, '  if (this->fArrowFadeDir < 0) {\n    this->mult = (short)(0x80 - (uint)(ushort)this->fArrowFade);\n    clampVal = (((((int)this->mult > 0) ? (int)this->mult : 0) < 0x40)\n                    ? (((int)this->mult > 0) ? (int)this->mult : 0) : 0x40);\n  }\n  else {\n    clampVal = 0x40;\n  }\n  this->mult = clampVal;\n  {\n    bool horiz = false;\n    this->HorzVertLine(ArrowLoc,horiz);\n  }'),
]
