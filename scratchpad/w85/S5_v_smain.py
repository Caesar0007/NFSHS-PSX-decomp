OLD = '  if (this->fState != kScreenMain_Credits) {\n    goto test_credits_menu;\n  }\n  if (FEApp->fCurrentMenu[0] != (tMenu *)&menuDefs->menuCredits) {\n    this->SetState(kScreenMain_StaticImage);\n    goto credits_state_done;\n  }\n  __asm__("" : : "i"(0));\ntest_credits_menu:\n  if (FEApp->fCurrentMenu[0] == (tMenu *)&menuDefs->menuCredits) {\n    this->SetState(kScreenMain_Credits);\n  }\ncredits_state_done:'
VARIANTS = [
  ('M0 fence gone', OLD, '  if (this->fState != kScreenMain_Credits) {\n    goto test_credits_menu;\n  }\n  if (FEApp->fCurrentMenu[0] != (tMenu *)&menuDefs->menuCredits) {\n    this->SetState(kScreenMain_StaticImage);\n    goto credits_state_done;\n  }\ntest_credits_menu:\n  if (FEApp->fCurrentMenu[0] == (tMenu *)&menuDefs->menuCredits) {\n    this->SetState(kScreenMain_Credits);\n  }\ncredits_state_done:'),
  ('M1 alias on 2nd test', OLD, '  extern tFEApplication *FEAppSM[] asm("FEApp");\n  if (this->fState != kScreenMain_Credits) {\n    goto test_credits_menu;\n  }\n  if (FEApp->fCurrentMenu[0] != (tMenu *)&menuDefs->menuCredits) {\n    this->SetState(kScreenMain_StaticImage);\n    goto credits_state_done;\n  }\ntest_credits_menu:\n  if (FEAppSM[0]->fCurrentMenu[0] == (tMenu *)&menuDefs->menuCredits) {\n    this->SetState(kScreenMain_Credits);\n  }\ncredits_state_done:'),
  ('M2 alias on 1st test', OLD, '  extern tFEApplication *FEAppSM[] asm("FEApp");\n  if (this->fState != kScreenMain_Credits) {\n    goto test_credits_menu;\n  }\n  if (FEAppSM[0]->fCurrentMenu[0] != (tMenu *)&menuDefs->menuCredits) {\n    this->SetState(kScreenMain_StaticImage);\n    goto credits_state_done;\n  }\ntest_credits_menu:\n  if (FEApp->fCurrentMenu[0] == (tMenu *)&menuDefs->menuCredits) {\n    this->SetState(kScreenMain_Credits);\n  }\ncredits_state_done:'),
  ('M3 alias both', OLD, '  extern tFEApplication *FEAppSM[] asm("FEApp");\n  if (this->fState != kScreenMain_Credits) {\n    goto test_credits_menu;\n  }\n  if (FEAppSM[0]->fCurrentMenu[0] != (tMenu *)&menuDefs->menuCredits) {\n    this->SetState(kScreenMain_StaticImage);\n    goto credits_state_done;\n  }\ntest_credits_menu:\n  if (FEAppSM[0]->fCurrentMenu[0] == (tMenu *)&menuDefs->menuCredits) {\n    this->SetState(kScreenMain_Credits);\n  }\ncredits_state_done:'),
  ('M4 nested if, no goto', OLD, '  if ((this->fState != kScreenMain_Credits) ||\n      (FEApp->fCurrentMenu[0] == (tMenu *)&menuDefs->menuCredits)) {\n    if (FEApp->fCurrentMenu[0] == (tMenu *)&menuDefs->menuCredits) {\n      this->SetState(kScreenMain_Credits);\n    }\n  }\n  else {\n    this->SetState(kScreenMain_StaticImage);\n  }\ncredits_state_done:'),
]
