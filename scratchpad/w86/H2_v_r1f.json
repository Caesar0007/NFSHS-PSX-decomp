[{"name": "T1 W2 + cardSlot=cardNum-1", "edits": [["        SavePinkSlipsCars(&carManager,&memCardData.carInfo,(short)(cardSlot >> 2),withoutCarInGarageNumber);", "        SavePinkSlipsCars(&carManager,&memCardData.carInfo,player,withoutCarInGarageNumber);"], ["    memCardResult = MCRD_handlecardevents(cardNum);", "    memCardResult = MCRD_handlecardevents(cardNum);\r\n    memCardResult = memCardResult ^ cardSlot;\r\n    memCardResult = memCardResult ^ cardSlot;"], ["  do { cardSlot = player * 4; } while (0);", "  do { cardSlot = cardNum + -1; } while (0);"]]}, {"name": "T2 W2 no do-while", "edits": [["        SavePinkSlipsCars(&carManager,&memCardData.carInfo,(short)(cardSlot >> 2),withoutCarInGarageNumber);", "        SavePinkSlipsCars(&carManager,&memCardData.carInfo,player,withoutCarInGarageNumber);"], ["    memCardResult = MCRD_handlecardevents(cardNum);", "    memCardResult = MCRD_handlecardevents(cardNum);\r\n    memCardResult = memCardResult ^ cardSlot;\r\n    memCardResult = memCardResult ^ cardSlot;"], ["  do { cardSlot = player * 4; } while (0);", "  cardSlot = player * 4;"]]}, {"name": "T3 W2 + dow over player copy", "edits": [["        SavePinkSlipsCars(&carManager,&memCardData.carInfo,(short)(cardSlot >> 2),withoutCarInGarageNumber);", "        SavePinkSlipsCars(&carManager,&memCardData.carInfo,player,withoutCarInGarageNumber);"], ["    memCardResult = MCRD_handlecardevents(cardNum);", "    memCardResult = MCRD_handlecardevents(cardNum);\r\n    memCardResult = memCardResult ^ cardSlot;\r\n    memCardResult = memCardResult ^ cardSlot;"], ["  do { cardSlot = player * 4; } while (0);", "  do { cardSlot = player * 4; } while (0);\r\n  do { cardSlot = cardSlot ^ 0; } while (0);"]]}, {"name": "T4 W2 + x2 on cardSlot preheader", "edits": [["        SavePinkSlipsCars(&carManager,&memCardData.carInfo,(short)(cardSlot >> 2),withoutCarInGarageNumber);", "        SavePinkSlipsCars(&carManager,&memCardData.carInfo,player,withoutCarInGarageNumber);"], ["    memCardResult = MCRD_handlecardevents(cardNum);", "    memCardResult = MCRD_handlecardevents(cardNum);\r\n    memCardResult = memCardResult ^ cardSlot;\r\n    memCardResult = memCardResult ^ cardSlot;"], ["  do { cardSlot = player * 4; } while (0);", "  do { cardSlot = player * 4; } while (0);\r\n  cardSlot = cardSlot ^ (int)player;\r\n  cardSlot = cardSlot ^ (int)player;"]]}]