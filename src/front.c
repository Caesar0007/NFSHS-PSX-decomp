#include "common.h"

/* func_80050810 (sjis2ascii) relocated to src/frontend/psx/memcard.c */
/* cont.34: the 18 front-overlay base-class nullsub virtual stubs were re-attributed
   (by SYM + VA-contiguity) to their owning modules: femenu(5) fescreen(3)
   femenuextended(2) femenudefs(3) fedialog(1) femenuoptions(1) fememcard(1)
   screencarselect(1) screencongrats(1). front overlay catch-all now empty. */
